import SwiftUI

struct LibraryView: View {
    var body: some View {
        ZStack {
            Color.spotifyBlack.ignoresSafeArea()
            
            Text("Library")
                .foregroundColor(.spotifyWhite)
        }
    }
}
