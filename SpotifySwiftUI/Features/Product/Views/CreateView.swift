import SwiftUI

struct CreateView: View {
    var body: some View {
        ZStack {
            Color.spotifyBlack.ignoresSafeArea()
            
            Text("Create")
                .foregroundColor(.spotifyWhite)
        }
    }
}
